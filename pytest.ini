[pytest]
minversion = 8.0
testpaths = tests
# Makes `pytest` and `python -m pytest` behave identically by putting the
# project root on sys.path regardless of how the suite is invoked.
pythonpath = .
addopts = -q --strict-markers --cov=aceest --cov-report=term-missing --cov-fail-under=90
